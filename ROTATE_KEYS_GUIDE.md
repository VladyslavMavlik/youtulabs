# 🔐 Покрокова інструкція ротації ключів (5 хвилин)

## Крок 1: Anthropic API Key (2 хвилини)

### 1.1 Відкрийте консоль
```
https://console.anthropic.com/settings/keys
```

### 1.2 Видаліть старий ключ
- Знайдіть ключ що починається на `sk-ant-api03-7l5nByA6...`
- Натисніть "Delete" або іконку смітника
- Підтвердіть видалення

### 1.3 Створіть новий ключ
- Натисніть "Create Key"
- Назва: `TextGeneratorGeis Production`
- Скопіюйте новий ключ (показується тільки раз!)

### 1.4 Оновіть .env
```bash
# Відкрийте файл:
nano /Users/mavlik/Projects/TextGeneratorGeis/.env

# Замініть рядок:
ANTHROPIC_API_KEY=новий_ключ_тут

# Збережіть: Ctrl+O, Enter, Ctrl+X
```

---

## Крок 2: Supabase Keys (3 хвилини)

### 2.1 Відкрийте Supabase Dashboard
```
https://supabase.com/dashboard/project/xcqjtdfvsgvuglllxgzc/settings/api
```

### 2.2 Reset Service Role Key
- Знайдіть секцію "service_role key"
- Натисніть "Reset" або "Regenerate"
- ⚠️ **ВАЖЛИВО:** Це зламає всі існуючі з'єднання на 1-2 хвилини
- Підтвердіть reset
- Скопіюйте новий ключ

### 2.3 Оновіть Backend .env
```bash
nano /Users/mavlik/Projects/TextGeneratorGeis/.env

# Замініть:
SUPABASE_SERVICE_ROLE_KEY=новий_service_role_ключ

# Збережіть: Ctrl+O, Enter, Ctrl+X
```

### 2.4 Перевірте чи змінився Anon Key
- На тій же сторінці перевірте "anon public" key
- Якщо він змінився - оновіть frontend .env:

```bash
nano /Users/mavlik/Projects/TextGeneratorGeis/Genisss-main/.env

# Замініть (якщо потрібно):
VITE_SUPABASE_ANON_KEY=новий_anon_ключ

# Збережіть: Ctrl+O, Enter, Ctrl+X
```

---

## Крок 3: Перезапуск (автоматично)

Після того як ви оновите .env файли, **НАПИШІТЬ МЕН "перезапусти все"** і я:

1. ✅ Зупиню всі сервіси
2. ✅ Перезапущу з новими ключами
3. ✅ Перевірю що все працює
4. ✅ Зроблю тестовий API виклик до Anthropic
5. ✅ Перевірю підключення до Supabase

---

## ❓ Питання та відповіді

**Q: Чи втрачу я дані після reset Supabase ключа?**
A: Ні! Дані в базі залишаються. Змінюється тільки ключ доступу.

**Q: Скільки часу не працюватиме сервіс?**
A: ~30 секунд для reset + 30 секунд для перезапуску = 1 хвилина

**Q: Чи можна відкотити назад?**
A: Ні, старий ключ видаляється назавжди. Але це ДОБРЕ - він був скомпрометований.

**Q: Чи потрібно оновлювати щось ще?**
A: Ні, тільки .env файли. Код вже використовує environment variables.

---

## 🆘 Якщо щось пішло не так

1. Перевірте чи файл .env існує:
   ```bash
   ls -la /Users/mavlik/Projects/TextGeneratorGeis/.env
   ```

2. Перевірте формат ключів (без пробілів, лапок):
   ```bash
   cat /Users/mavlik/Projects/TextGeneratorGeis/.env | grep API_KEY
   ```

3. Напишіть мені "допоможи" і я діагностую проблему

---

**Готовий? Виділи 5 хвилин і зроби це зараз! 🚀**

Після ротації напиши мені: **"перезапусти все"**
